---
import { isLocale } from "../i18n/locales";
import { useTranslation } from "../i18n";
import { years } from "../data/experience";

const locale = isLocale(Astro.currentLocale) ? Astro.currentLocale : "en";
const t = useTranslation(locale);

const xp = years();
---

<div>
  <h1 class="text-display">{t("intro.greeting")} <span>Diego</span></h1>
  <p class="text-display-sub">{t("intro.role")}</p>
  <p>{t("intro.aboutProfessional", { xp })}</p>
  <p>{t("intro.aboutCasual")}</p>
</div>

<style>
  .text-display-sub {
    margin: var(--space-lg) 0;
  }
</style>
